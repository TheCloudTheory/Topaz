@page "/subscriptions"
@using Topaz.Portal.Models.Subscriptions
@inject TopazClient TopazClient

<div class="d-flex align-items-center justify-content-between mb-3">
    <h1 class="mb-0">Subscriptions</h1>

    <button class="btn btn-primary btn-sm"
            @onclick="ToggleCreate"
            disabled="@(_loading || _creatingBusy)">
        New subscription
    </button>
</div>

<div class="row g-3">
    <div class="@(_showCreate || _editingSubscription is not null ? "col-12 col-lg-8" : "col-12")">
        <div class="card h-100">
            <div class="card-body">
                @if (_loading)
                {
                    <p>Loading…</p>
                }
                else if (_error is not null)
                {
                    <div class="alert alert-danger" role="alert">
                        @_error
                    </div>
                }
                else if (_subscriptions.Length == 0)
                {
                    <p>No subscriptions found.</p>
                }
                else
                {
                    <div class="table-responsive">
                        <table class="table table-sm table-hover align-middle mb-0">
                            <thead>
                            <tr>
                                <th>Display name</th>
                                <th>Subscription ID</th>
                                <th class="text-muted">Resource ID</th>
                                <th></th>
                            </tr>
                            </thead>
                            <tbody>
                            @foreach (var s in _subscriptions)
                            {
                                <tr>
                                    <td>
                                        @if (Guid.TryParse(s.SubscriptionId, out var subId))
                                        {
                                            <a href="@GetSubscriptionHref(subId)">
                                                @(s.DisplayName ?? "(no name)")
                                            </a>
                                        }
                                        else
                                        {
                                            @(s.DisplayName ?? "(no name)")
                                        }
                                    </td>
                                    <td><code>@(s.SubscriptionId ?? "(unknown)")</code></td>
                                    <td class="text-muted"><small>@(s.Id ?? "")</small></td>
                                    <td class="text-end">
                                        <button class="btn btn-sm btn-outline-primary"
                                                @onclick="() => BeginEdit(s)"
                                                disabled="@(_loading || _creatingBusy || _editingBusy)">
                                            Edit
                                        </button>
                                    </td>
                                </tr>
                            }
                            </tbody>
                        </table>
                    </div>
                }
            </div>
        </div>
    </div>

    @if (_showCreate)
    {
        <div class="col-12 col-lg-4">
            <div class="card h-100">
                <div class="card-body">
                    <div class="d-flex align-items-start justify-content-between mb-3">
                        <h5 class="card-title mb-0">Create subscription</h5>

                        <button class="btn btn-sm btn-outline-secondary"
                                @onclick="CloseCreate"
                                disabled="@_creatingBusy">
                            Close
                        </button>
                    </div>

                    @if (!string.IsNullOrWhiteSpace(_createError))
                    {
                        <div class="alert alert-danger" role="alert">
                            @_createError
                        </div>
                    }

                    <div class="mb-3">
                        <label class="form-label">Display name</label>
                        <input class="form-control"
                               @bind="_newSubscriptionName"
                               placeholder="e.g. Dev Subscription"
                               disabled="@_creatingBusy" />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Subscription ID (optional)</label>
                        <input class="form-control"
                               @bind="_newSubscriptionIdText"
                               placeholder="leave empty to auto-generate"
                               title="If empty, a new GUID will be generated."
                               disabled="@_creatingBusy" />
                    </div>

                    <div class="d-grid gap-2">
                        <button class="btn btn-success"
                                @onclick="CreateSubscriptionAsync"
                                disabled="@_creatingBusy">
                            Create
                        </button>

                        <button class="btn btn-outline-secondary"
                                @onclick="CloseCreate"
                                disabled="@_creatingBusy">
                            Cancel
                        </button>
                    </div>
                </div>
            </div>
        </div>
    }

    @if (_editingSubscription is not null)
    {
        <div class="col-12 col-lg-4">
            <div class="card h-100">
                <div class="card-body">
                    <div class="d-flex align-items-start justify-content-between mb-3">
                        <h5 class="card-title mb-0">Edit subscription</h5>

                        <button class="btn btn-sm btn-outline-secondary"
                                @onclick="CloseEdit"
                                disabled="@_editingBusy">
                            Close
                        </button>
                    </div>

                    @if (!string.IsNullOrWhiteSpace(_editError))
                    {
                        <div class="alert alert-danger" role="alert">
                            @_editError
                        </div>
                    }

                    <div class="mb-3">
                        <label class="form-label">Subscription ID</label>
                        <input class="form-control"
                               value="@_editingSubscription.SubscriptionId"
                               disabled="true"
                               readonly />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Display name</label>
                        <input class="form-control"
                               @bind="_editDisplayName"
                               placeholder="Enter display name"
                               disabled="@_editingBusy" />
                    </div>

                    <div class="d-grid gap-2">
                        <button class="btn btn-success"
                                @onclick="SaveSubscriptionAsync"
                                disabled="@_editingBusy">
                            Save
                        </button>

                        <button class="btn btn-outline-secondary"
                                @onclick="CloseEdit"
                                disabled="@_editingBusy">
                            Cancel
                        </button>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@code
{
    private bool _loading = true;
    private string? _error;
    private SubscriptionDto[] _subscriptions = [];

    private bool _showCreate;
    private bool _creatingBusy;
    private string? _createError;
    private string _newSubscriptionName = "";
    private string _newSubscriptionIdText = "";

    private SubscriptionDto? _editingSubscription;
    private bool _editingBusy;
    private string? _editError;
    private string _editDisplayName = "";

    protected override async Task OnInitializedAsync()
    {
        await LoadAsync();
    }

    private string GetSubscriptionHref(Guid subscriptionId)
        => $"/subscriptions/{subscriptionId:D}";

    private async Task LoadAsync()
    {
        _loading = true;
        _error = null;

        try
        {
            var resp = await TopazClient.ListSubscriptions();
            _subscriptions = resp?.Value ?? [];
        }
        catch (Exception ex)
        {
            _error = $"Failed to load subscriptions: {ex.Message}";
        }
        finally
        {
            _loading = false;
        }
    }

    private void ToggleCreate()
    {
        _showCreate = !_showCreate;
        _createError = null;
    }

    private void CloseCreate()
    {
        _showCreate = false;
        _createError = null;
    }

    private void BeginEdit(SubscriptionDto subscription)
    {
        _editingSubscription = subscription;
        _editDisplayName = subscription.DisplayName ?? "";
        _editError = null;
        _showCreate = false;
    }

    private void CloseEdit()
    {
        _editingSubscription = null;
        _editDisplayName = "";
        _editError = null;
    }

    private async Task SaveSubscriptionAsync()
    {
        if (_editingSubscription is null)
            return;

        _editError = null;

        if (string.IsNullOrWhiteSpace(_editDisplayName))
        {
            _editError = "Please enter a display name.";
            return;
        }

        if (!Guid.TryParse(_editingSubscription.SubscriptionId, out var subscriptionId))
        {
            _editError = "Invalid subscription ID.";
            return;
        }

        _editingBusy = true;
        try
        {
            await TopazClient.UpdateSubscriptionDisplayName(subscriptionId, _editDisplayName);

            _editingSubscription = null;
            _editDisplayName = "";

            await LoadAsync();
        }
        catch (Exception ex)
        {
            _editError = ex.Message;
        }
        finally
        {
            _editingBusy = false;
        }
    }

    private async Task CreateSubscriptionAsync()
    {
        _createError = null;

        if (string.IsNullOrWhiteSpace(_newSubscriptionName))
        {
            _createError = "Please enter a display name.";
            return;
        }

        Guid subscriptionId;
        if (string.IsNullOrWhiteSpace(_newSubscriptionIdText))
        {
            subscriptionId = Guid.NewGuid();
        }
        else if (!Guid.TryParse(_newSubscriptionIdText, out subscriptionId))
        {
            _createError = "Subscription ID must be a valid GUID (or leave it empty).";
            return;
        }

        _creatingBusy = true;
        try
        {
            await TopazClient.CreateSubscription(subscriptionId, _newSubscriptionName);

            _newSubscriptionName = "";
            _newSubscriptionIdText = "";
            _showCreate = false;

            await LoadAsync();
        }
        catch (Exception ex)
        {
            _createError = ex.Message;
        }
        finally
        {
            _creatingBusy = false;
        }
    }
}