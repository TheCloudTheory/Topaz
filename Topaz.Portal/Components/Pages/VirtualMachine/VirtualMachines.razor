@page "/portal/virtual-machines"
@using System.Net
@using Topaz.Portal.Models.VirtualMachines
@using Topaz.Portal.Models.ResourceGroups
@using Topaz.Portal.Models.Subscriptions
@using Topaz.Portal.Components.Shared
@inject ITopazClient TopazClient
@inject NavigationManager Nav

<PageTitle>Virtual machines — Topaz Portal</PageTitle>

<div class="d-flex align-items-center justify-content-between mb-3">
    <h1 class="mb-0">Virtual machines</h1>

    <button class="btn btn-primary btn-sm"
            @onclick="ToggleCreate"
            disabled="@(_loading || _creatingBusy)">
        New virtual machine
    </button>
</div>

<div class="row g-3">
    <div class="@(_showCreate ? "col-12 col-lg-8" : "col-12")">
        <div class="card">
            <div class="card-body">
                <div class="d-flex align-items-center justify-content-between mb-3">
                    <div class="d-flex align-items-center gap-2">
                        <strong>Filter</strong>
                        <span class="text-muted small">
                            @(_selectedSubscriptionIds.Count == 0 ? "All subscriptions" : $"{_selectedSubscriptionIds.Count} selected")
                        </span>
                    </div>

                    <div class="d-flex align-items-center gap-2">
                        <button class="btn btn-sm btn-outline-secondary"
                                @onclick="ToggleFilter"
                                disabled="@_loading">
                            @(_showFilter ? "Hide" : "Show")
                        </button>

                        <button class="btn btn-sm btn-outline-secondary"
                                @onclick="ClearFilter"
                                disabled="@(_loading || _selectedSubscriptionIds.Count == 0)">
                            Clear
                        </button>
                    </div>
                </div>

                @if (_showFilter)
                {
                    <div class="border rounded p-2 mb-3" style="max-height: 180px; overflow:auto;">
                        @if (_subscriptions.Length == 0)
                        {
                            <div class="text-muted small">No subscriptions loaded.</div>
                        }
                        else
                        {
                            @foreach (var s in _subscriptions)
                            {
                                var id = s.SubscriptionId ?? "";
                                var isChecked = !string.IsNullOrWhiteSpace(id) && _selectedSubscriptionIds.Contains(id);

                                <div class="form-check">
                                    <input class="form-check-input"
                                           type="checkbox"
                                           id="@($"sub-filter-{id}")"
                                           checked="@isChecked"
                                           @onchange="(e) => OnSubscriptionFilterChanged(id, e)" />
                                    <label class="form-check-label" for="@($"sub-filter-{id}")">
                                        @(s.DisplayName ?? "(no name)")
                                        <span class="text-muted small">(@id)</span>
                                    </label>
                                </div>
                            }
                        }
                    </div>
                }

                @if (_loading)
                {
                    <p>Loading…</p>
                }
                else if (_error is not null)
                {
                    <div class="alert alert-danger" role="alert">
                        @_error
                    </div>
                }
                else if (FilteredVms.Length == 0)
                {
                    <p class="mb-0">No virtual machines found.</p>
                }
                else
                {
                    <div class="table-responsive">
                        <table class="table table-sm table-hover align-middle mb-0">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Resource group</th>
                                    <th>Subscription</th>
                                    <th>Location</th>
                                    <th>Size</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var vm in FilteredVms)
                                {
                                    <tr>
                                        <td>
                                            @if (Guid.TryParse(vm.SubscriptionId, out var subId) &&
                                                 !string.IsNullOrWhiteSpace(vm.ResourceGroupName) &&
                                                 !string.IsNullOrWhiteSpace(vm.Name))
                                            {
                                                <a href="@GetVmHref(subId, vm.ResourceGroupName, vm.Name)">@vm.Name</a>
                                            }
                                            else
                                            {
                                                @(vm.Name ?? "(unknown)")
                                            }
                                        </td>
                                        <td>@(vm.ResourceGroupName ?? "-")</td>
                                        <td>@(vm.SubscriptionName ?? "(unknown)")</td>
                                        <td><code>@(vm.Location ?? "-")</code></td>
                                        <td>@(vm.VmSize ?? "-")</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
            </div>
        </div>
    </div>

    @if (_showCreate)
    {
        <div class="col-12 col-lg-4">
            <div class="card h-100">
                <div class="card-body">
                    <div class="d-flex align-items-start justify-content-between mb-3">
                        <h5 class="card-title mb-0">Create virtual machine</h5>

                        <button class="btn btn-sm btn-outline-secondary"
                                @onclick="CloseCreate"
                                disabled="@_creatingBusy">
                            Close
                        </button>
                    </div>

                    @if (!string.IsNullOrWhiteSpace(_createError))
                    {
                        <div class="alert alert-danger" role="alert">
                            @_createError
                        </div>
                    }

                    <div class="mb-3">
                        <label class="form-label">Subscription</label>
                        <select class="form-select"
                                @bind="_createSubscriptionId"
                                @bind:after="OnCreateSubscriptionChangedAsync"
                                disabled="@_creatingBusy">
                            <option value="">Select a subscription…</option>
                            @foreach (var s in _subscriptions)
                            {
                                <option value="@(s.SubscriptionId ?? "")">
                                    @(s.DisplayName ?? "(no name)") (@(s.SubscriptionId ?? ""))
                                </option>
                            }
                        </select>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Resource group</label>
                        <select class="form-select"
                                @bind="_createResourceGroupName"
                                disabled="@(_creatingBusy || _rgLoading || string.IsNullOrWhiteSpace(_createSubscriptionId))">
                            <option value="">@(_rgLoading ? "Loading…" : (string.IsNullOrWhiteSpace(_createSubscriptionId) ? "Select a subscription first" : "Select a resource group…"))</option>
                            @foreach (var rg in _resourceGroupOptions)
                            {
                                <option value="@(rg.Name ?? "")">@(rg.Name ?? "")</option>
                            }
                        </select>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">VM name</label>
                        <input class="form-control"
                               @bind="_createVmName"
                               placeholder="e.g. my-vm"
                               disabled="@_creatingBusy" />
                    </div>

                    <LocationDropdown
                        SelectedLocation="@_createLocation"
                        OnLocationChanged="OnLocationChanged"
                        Disabled="@_creatingBusy" />

                    <div class="mb-3">
                        <label class="form-label">VM size</label>
                        <select class="form-select" @bind="_createVmSize" disabled="@_creatingBusy">
                            <option value="Standard_B2s">Standard_B2s (2 vCPUs, 4 GiB RAM)</option>
                            <option value="Standard_B4ms">Standard_B4ms (4 vCPUs, 16 GiB RAM)</option>
                            <option value="Standard_D2s_v3">Standard_D2s_v3 (2 vCPUs, 8 GiB RAM)</option>
                            <option value="Standard_D4s_v3">Standard_D4s_v3 (4 vCPUs, 16 GiB RAM)</option>
                            <option value="Standard_D2SV3">Standard_D2SV3</option>
                            <option value="Standard_F2s_v2">Standard_F2s_v2 (2 vCPUs, 4 GiB RAM)</option>
                        </select>
                    </div>

                    <div class="d-grid gap-2 mt-3">
                        <button class="btn btn-success"
                                @onclick="CreateVmAsync"
                                disabled="@_creatingBusy">
                            @(_creatingBusy ? "Creating…" : "Create")
                        </button>

                        <button class="btn btn-outline-secondary"
                                @onclick="CloseCreate"
                                disabled="@_creatingBusy">
                            Cancel
                        </button>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@code {
    private bool _loading = true;
    private string? _error;
    private VirtualMachineDto[] _vms = [];
    private SubscriptionDto[] _subscriptions = [];
    private bool _showFilter = true;
    private readonly HashSet<string> _selectedSubscriptionIds = new(StringComparer.OrdinalIgnoreCase);

    private bool _showCreate;
    private bool _creatingBusy;
    private string? _createError;
    private string _createSubscriptionId = "";
    private string _createResourceGroupName = "";
    private string _createVmName = "";
    private string _createLocation = "westeurope";
    private string _createVmSize = "Standard_B2s";
    private bool _rgLoading;
    private ResourceGroupDto[] _resourceGroupOptions = [];

    private VirtualMachineDto[] FilteredVms =>
        _selectedSubscriptionIds.Count == 0
            ? _vms
            : _vms.Where(v =>
                !string.IsNullOrWhiteSpace(v.SubscriptionId) &&
                _selectedSubscriptionIds.Contains(v.SubscriptionId))
            .ToArray();

    protected override async Task OnInitializedAsync()
    {
        await LoadAsync();
    }

    private async Task LoadAsync()
    {
        _loading = true;
        _error = null;

        try
        {
            var subsTask = TopazClient.ListSubscriptions();
            var vmsTask = TopazClient.ListVirtualMachines();

            await Task.WhenAll(subsTask, vmsTask);

            _subscriptions = subsTask.Result.Value;
            _vms = vmsTask.Result.Value;
        }
        catch (Exception ex)
        {
            _error = ex.Message;
        }
        finally
        {
            _loading = false;
        }
    }

    private static string GetVmHref(Guid subscriptionId, string resourceGroupName, string vmName)
        => $"/portal/virtual-machines/{subscriptionId:D}/{WebUtility.UrlEncode(resourceGroupName)}/{WebUtility.UrlEncode(vmName)}";

    private void ToggleFilter() => _showFilter = !_showFilter;
    private void ClearFilter() => _selectedSubscriptionIds.Clear();
    private void OnLocationChanged(string location) => _createLocation = location;

    private void OnSubscriptionFilterChanged(string subscriptionId, ChangeEventArgs e)
    {
        var isChecked = e.Value is true;
        if (isChecked)
            _selectedSubscriptionIds.Add(subscriptionId);
        else
            _selectedSubscriptionIds.Remove(subscriptionId);
    }

    private void ToggleCreate()
    {
        _showCreate = !_showCreate;
        _createError = null;
    }

    private void CloseCreate()
    {
        _showCreate = false;
        _createError = null;
        _createSubscriptionId = "";
        _createResourceGroupName = "";
        _createVmName = "";
        _createLocation = "westeurope";
        _createVmSize = "Standard_B2s";
        _resourceGroupOptions = [];
    }

    private async Task OnCreateSubscriptionChangedAsync()
    {
        _createResourceGroupName = "";
        _resourceGroupOptions = [];

        if (!Guid.TryParse(_createSubscriptionId, out var subId))
            return;

        _rgLoading = true;
        try
        {
            var rgs = await TopazClient.ListResourceGroups(subId);
            _resourceGroupOptions = rgs.Value;
        }
        catch
        {
            _resourceGroupOptions = [];
        }
        finally
        {
            _rgLoading = false;
        }
    }

    private async Task CreateVmAsync()
    {
        _createError = null;

        if (string.IsNullOrWhiteSpace(_createSubscriptionId))
        {
            _createError = "Please select a subscription.";
            return;
        }

        if (!Guid.TryParse(_createSubscriptionId, out var subscriptionId))
        {
            _createError = "Subscription ID is invalid.";
            return;
        }

        if (string.IsNullOrWhiteSpace(_createResourceGroupName))
        {
            _createError = "Please select a resource group.";
            return;
        }

        if (string.IsNullOrWhiteSpace(_createVmName))
        {
            _createError = "Please enter a VM name.";
            return;
        }

        if (string.IsNullOrWhiteSpace(_createLocation))
        {
            _createError = "Please select a location.";
            return;
        }

        _creatingBusy = true;
        try
        {
            await TopazClient.CreateVirtualMachine(
                subscriptionId,
                _createResourceGroupName,
                _createVmName,
                _createLocation,
                _createVmSize);

            _createVmName = "";
            _createResourceGroupName = "";
            _showCreate = false;

            await LoadAsync();
        }
        catch (Exception ex)
        {
            _createError = ex.Message;
        }
        finally
        {
            _creatingBusy = false;
        }
    }
}
