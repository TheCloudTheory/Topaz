@using System.Net

<div class="row g-3">
    <div class="col-12 col-lg-3">
        <div class="card">
            <div class="card-body">
                <div class="list-group list-group-flush">
                    <NavLink class="list-group-item list-group-item-action"
                             href="@OverviewHref"
                             Match="NavLinkMatch.All">
                        Overview
                    </NavLink>

                    <NavLink class="list-group-item list-group-item-action"
                             href="@IamHref">
                        IAM
                    </NavLink>

                    <NavLink class="list-group-item list-group-item-action"
                             href="@TagsHref">
                        Tags
                    </NavLink>

                    <div class="list-group-item text-muted small fw-semibold text-uppercase py-1 px-3 mt-2" style="font-size:0.7rem;">
                        Settings
                    </div>

                    <NavLink class="list-group-item list-group-item-action"
                             href="@PropertiesHref">
                        Properties
                    </NavLink>

                    <div class="list-group-item text-muted small fw-semibold text-uppercase py-1 px-3 mt-2" style="font-size:0.7rem;">
                        Entities
                    </div>

                    <NavLink class="list-group-item list-group-item-action"
                             href="@QueuesHref">
                        Queues
                    </NavLink>

                    <NavLink class="list-group-item list-group-item-action"
                             href="@TopicsHref">
                        Topics
                    </NavLink>
                </div>
            </div>
        </div>
    </div>

    <div class="col-12 col-lg-9">
        @ChildContent
    </div>
</div>

@code {
    [Parameter] public Guid SubscriptionId { get; set; }
    [Parameter] public string ResourceGroupName { get; set; } = "";
    [Parameter] public string NamespaceName { get; set; } = "";
    [Parameter] public RenderFragment ChildContent { get; set; } = default!;

    private string EncodedRg => WebUtility.UrlEncode(ResourceGroupName);
    private string EncodedName => WebUtility.UrlEncode(NamespaceName);

    private string Base => $"/portal/service-bus/{SubscriptionId:D}/{EncodedRg}/{EncodedName}";

    private string OverviewHref => Base;
    private string IamHref => $"{Base}/iam";
    private string TagsHref => $"{Base}/tags";
    private string PropertiesHref => $"{Base}/properties";
    private string QueuesHref => $"{Base}/queues";
    private string TopicsHref => $"{Base}/topics";
}
